import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DPSA4-MusicApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
